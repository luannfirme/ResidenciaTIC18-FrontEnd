import { LacoRepeticaoDirective } from './laco-repeticao.directive';

describe('LacoRepeticaoDirective', () => {
  it('should create an instance', () => {
    const directive = new LacoRepeticaoDirective();
    expect(directive).toBeTruthy();
  });
});
